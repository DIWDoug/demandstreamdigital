import ServicePageLayout from "@/components/services/ServicePageLayout";
import { paidMediaConfig } from "@/data/service-pages/paid-media";

const PaidMedia = () => {
  return <ServicePageLayout config={paidMediaConfig} />;
};

export default PaidMedia;
